{% set templates = [
    'template-parts/twig/header.twig',
    'template-parts/twig/main-loop.twig',
    'template-parts/twig/footer.twig'
] %}

{# Loop the templates #}
{% for template in templates %}
    {% include template %}
{% endfor %}